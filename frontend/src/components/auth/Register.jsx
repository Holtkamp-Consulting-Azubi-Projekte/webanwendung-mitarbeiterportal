/**
 * `Register` Komponente für das Registrierungsformular.
 * Ermöglicht neuen Benutzern die Erstellung eines Kontos.
 * Interagiert mit dem Backend für die Registrierung.
 */
const Register = ({ onSwitchToLogin }) => {
  // ... existing code ...
} 