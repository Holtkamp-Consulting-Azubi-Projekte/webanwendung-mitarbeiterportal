/**
 * `Login` Komponente für das Login-Formular.
 * Ermöglicht Benutzern die Anmeldung mit E-Mail und Passwort.
 * Interagiert mit dem Backend für die Authentifizierung.
 */
const Login = ({ onSuccess, onSwitchToRegister }) => {
  // ... existing code ...
} 